/* STUB temporal de DIAG */
const DIAG = {
  render(b,t,d,ctx){ return '<div class="card"><div class="card-b"><div class="empty">diagrama '+esc(b.kind)+'</div></div></div>'; },
  svg(){ return ''; }, bind(){}, setPath(){}, toText(){ return ''; }
};
